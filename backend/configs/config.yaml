
server:
  port: "8080"
  env: "development"

database:
  host: "localhost"
  port: 5432
  user: "fitness_user"
  password: "your_password"
  dbname: "fitness_app"

crm:
  base_url: "https://yourstudio.impulsecrm.ru/api/v1/"
  api_key: "your_api_key_here"

redis:
  host: "localhost"
  port: 6379
  password: ""

sms:
  provider: "twilio"  # или smsc.ru
  sid: ""
  token: ""
  from_number: ""

notifications:
  telegram_bot_token: ""
  firebase_credentials_path: "./firebase-credentials.json"

features:
  enable_streaks: true
  enable_points: true
  enable_measurements: true